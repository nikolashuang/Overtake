local ParkourController = {}

function ParkourController.AttemptVault(input, processed)
    if processed then return end

    if input.KeyCode ~= Enum.KeyCode.Space then
        return false
    end

    
    
end


return ParkourController